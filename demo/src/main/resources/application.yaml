server:
    port: 8080


spring:
    application:
        name: "demo"
    data:
        redis:
            host: localhost
            port: 6379

aws:
    region: "us-east-1"
    endpoint-url: "http://localhost:8000"
    dynamodb:
        chat-table: "Chat"
        message-table: "Message"
        members-table: "Members"
        user-table: "User"